import React from 'react';

import { LoaderOverlay, LoaderContainer } from './loader.styles';

const Loader = () => (
  <LoaderOverlay>
    <LoaderContainer />
  </LoaderOverlay>
);

export default Loader;
